import s from './ipPanel.module.scss'

/** Компонент левой части панели с IP */
export const IpPanel = () => {
    return (
        <div className={s.panel}>
            Компонент с IP и логотипом
        </div>
    );
};
