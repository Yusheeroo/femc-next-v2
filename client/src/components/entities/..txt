 🗂 entities/          # Бизнес-сущности
 └── user/           # Пример сущности
      ├── model/      # Логика и типы
      └── ui/        # Компоненты сущности

entities – Бизнес-сущности

Что хранится:
Сущности, не зависящие от фич (User, Product, Order).
Их модели, типы, базовые компоненты.

Пример:
export const UserAvatar = ({ user }) => <img src={user.avatar} />;