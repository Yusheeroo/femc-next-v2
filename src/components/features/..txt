├── 🗂 features/          # Фичи (user scenarios)
    └── auth/           # Пример фичи
         ├── login-form/  # Компоненты фичи
         └── register-form/

features – Пользовательские сценарии

Что хранится:
Логика и компоненты для конкретных фич (auth, cart, product-search).

Пример : 
export const LoginForm = () => {
  const [email, setEmail] = useState('');
  return <form>{/* ... */}</form>;
};